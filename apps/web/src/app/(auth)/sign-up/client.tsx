import { SignUpForm } from '@/features/auth';

const SignUpPageClient = () => {
  return (
    <main className="relative flex min-h-screen items-center justify-center overflow-hidden">
      <SignUpForm />
    </main>
  );
};

export { SignUpPageClient };
